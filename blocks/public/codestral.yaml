name: Codestral
version: 1.0.1
schema: v1

models:
  - name: Codestral
    provider: mistral
    model: codestral-latest
    apiKey: ${{ inputs.MISTRAL_API_KEY }}
    roles:
      - autocomplete
      - chat
      - edit
      - apply
